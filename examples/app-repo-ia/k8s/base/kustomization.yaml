apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: ia-service-base

resources:
  - deployment.yaml
  - service.yaml
  - serviceaccount.yaml
  - qdrant-statefulset.yaml

commonLabels:
  app.kubernetes.io/name: ia-service
  app.kubernetes.io/part-of: arch-analyzer
