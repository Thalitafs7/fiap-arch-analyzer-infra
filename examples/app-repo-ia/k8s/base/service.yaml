apiVersion: v1
kind: Service
metadata:
  name: ia-service
  labels:
    app: ia-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: ia-service
